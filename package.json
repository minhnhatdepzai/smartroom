{
  "name": "eduvision-ai-cinematic-landing-v3",
  "private": true,
  "version": "0.3.1",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "deploy:check": "npm run build && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
